/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "img.freepik.com",
      "images.pexels.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
